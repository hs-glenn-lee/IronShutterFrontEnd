export default class PageParameter {
  constructor (page, size, direction, sortProperty) {
    this.page = page
    this.size = size
    this.direction = direction
    this.sortProperty = sortProperty
  }
}
